@model TourAgency.Models.User
<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Registration</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
</head>
<body>

<div class="container-fluid" align="center">
    <div class="row align-items-center" style="height: 100vh">
        <div class="col">
            <h1>Registration</h1>
            @using(Html.BeginForm("registration", "Registration", FormMethod.Post)) {
                <table class="table-borderless" align="center">
                    <tr>
                        <td colspan="2">
                            @{
                                if (ViewData.ContainsKey("exception")) {
                                    <div class="alert alert-danger" role="alert">
                                        @ViewData.Eval("exception")
                                    </div>
                                }
                            }
                        </td>
                    </tr>
                    
                    <tr>
                        <td><label style="margin: 5px 5px 5px"> First Name: </label></td>
                        <td>@Html.TextBoxFor(model => model.firstName)</td>
                    </tr>
                    <tr>
                        <td><label style="margin: 5px 5px 5px"> Last Name: </label></td>
                        <td>@Html.TextBoxFor(model => model.lastName)</td>
                    </tr>
                    <tr>
                        <td><label style="margin: 5px 5px 5px"> Email: </label></td>
                        <td>@Html.TextBoxFor(model => model.email)</td>
                    </tr>
                    <tr>
                        <td><label style="margin: 5px 5px 5px"> Password: </label></td>
                        <td>@Html.PasswordFor(model => model.password)</td>
                    </tr>
                    <tr>
                        <td><label style="margin: 5px 5px 5px"> telephone: </label></td>
                        <td>@Html.TextBoxFor(model => model.telephone)</td>
                    </tr>
                    <tr>
                        <td colspan="2" align="center">
                            <input style="margin: 5px 5px 5px" type="submit" class="btn btn-info" value="Sign Up"/>
                            <input style="margin: 5px 5px 5px" type="button" class="btn btn-info" value="Cancel"
                                   onclick='location.href = "/login"'/>
                        </td>
                    </tr>
                </table>
            }
            </div>
        </div>
    </div>
</body>
</html>